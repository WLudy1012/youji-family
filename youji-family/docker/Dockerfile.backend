# 由基家族网站系统 - 后端 Dockerfile

FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY src ./src

# 创建上传目录
RUN mkdir -p uploads

# 暴露端口
EXPOSE 8080

# 启动命令
CMD ["node", "src/app.js"]
